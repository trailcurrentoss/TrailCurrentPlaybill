{
  "name": "trailcurrent-playbill-controller",
  "version": "0.1.0",
  "description": "TrailCurrent Playbill — always-on control daemon. MQTT subscriber, source-plugin host, mpv owner. The Electron GUI is one of its clients.",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "bonjour-service": "^1.3.0",
    "mqtt": "^5.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
