{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "trailcurrent-playbill-commands",
  "title": "TrailCurrent Playbill — command bus payloads",
  "description": "Shape of every command accepted by the controller's CommandBus, regardless of origin (local IPC, MQTT, internal). Validated at the bus boundary by Ajv before dispatch. Each command's `x-class` field is informational: 'A' = also exists as a CAN message in TrailCurrent.dbc (see x-dbc); 'B' = MQTT/IPC only.",

  "oneOf": [
    { "$ref": "#/$defs/SystemEcho" },
    { "$ref": "#/$defs/SystemCommands" },
    { "$ref": "#/$defs/SystemLaunchGui" },
    { "$ref": "#/$defs/SystemQuitGui" },
    { "$ref": "#/$defs/SystemFocus" },
    { "$ref": "#/$defs/SystemWake" },
    { "$ref": "#/$defs/SystemSleep" },

    { "$ref": "#/$defs/SettingsGet" },
    { "$ref": "#/$defs/SettingsReplace" },
    { "$ref": "#/$defs/SettingsPatch" },

    { "$ref": "#/$defs/ConnectionGet" },
    { "$ref": "#/$defs/ConnectionSet" },
    { "$ref": "#/$defs/ConnectionClear" },
    { "$ref": "#/$defs/ConnectionSetCa" },

    { "$ref": "#/$defs/NavDpad" },
    { "$ref": "#/$defs/NavText" },

    { "$ref": "#/$defs/TransportPlay" },
    { "$ref": "#/$defs/TransportPause" },
    { "$ref": "#/$defs/TransportStop" },
    { "$ref": "#/$defs/TransportToggle" },
    { "$ref": "#/$defs/TransportNext" },
    { "$ref": "#/$defs/TransportPrevious" },
    { "$ref": "#/$defs/TransportSeekRel" },
    { "$ref": "#/$defs/TransportSeekAbs" },
    { "$ref": "#/$defs/TransportVolumeUp" },
    { "$ref": "#/$defs/TransportVolumeDown" },
    { "$ref": "#/$defs/TransportVolumeSet" },
    { "$ref": "#/$defs/TransportMuteOn" },
    { "$ref": "#/$defs/TransportMuteOff" },
    { "$ref": "#/$defs/TransportMuteToggle" },

    { "$ref": "#/$defs/RadioTune" },
    { "$ref": "#/$defs/RadioStop" },
    { "$ref": "#/$defs/RadioGetState" },
    { "$ref": "#/$defs/RadioScan" },
    { "$ref": "#/$defs/RadioListAdapters" },
    { "$ref": "#/$defs/RadioListPresets" },
    { "$ref": "#/$defs/RadioSetPresets" },
    { "$ref": "#/$defs/RadioLookupScanner" },
    { "$ref": "#/$defs/RadioProbeTools" },

    { "$ref": "#/$defs/LivetvTune" },
    { "$ref": "#/$defs/LivetvStopTune" },
    { "$ref": "#/$defs/LivetvScan" },
    { "$ref": "#/$defs/LivetvStopScan" },
    { "$ref": "#/$defs/LivetvListChannels" },
    { "$ref": "#/$defs/LivetvListAdapters" },
    { "$ref": "#/$defs/LivetvProbeTools" },

    { "$ref": "#/$defs/SourceLaunch" },
    { "$ref": "#/$defs/SourceList" },
    { "$ref": "#/$defs/SourceResolve" },
    { "$ref": "#/$defs/SourceSearch" },

    { "$ref": "#/$defs/YoutubeGetSettings" },
    { "$ref": "#/$defs/YoutubeSetSettings" },
    { "$ref": "#/$defs/YoutubeSignInStart" },
    { "$ref": "#/$defs/YoutubeSignInPoll" },
    { "$ref": "#/$defs/YoutubeSignInCancel" },
    { "$ref": "#/$defs/YoutubeSignOut" },
    { "$ref": "#/$defs/YoutubeGetAccount" },

    { "$ref": "#/$defs/HeadwatersGetSettings" },
    { "$ref": "#/$defs/HeadwatersSetSettings" },
    { "$ref": "#/$defs/HeadwatersClear" },
    { "$ref": "#/$defs/HeadwatersValidateApiKey" },

    { "$ref": "#/$defs/CastStart" },
    { "$ref": "#/$defs/CastStop" },
    { "$ref": "#/$defs/CastGetStatus" },

    { "$ref": "#/$defs/NetflixStart" },
    { "$ref": "#/$defs/NetflixStop" },
    { "$ref": "#/$defs/NetflixGetStatus" },

    { "$ref": "#/$defs/DvdGetStatus" },
    { "$ref": "#/$defs/DvdRefreshStatus" },
    { "$ref": "#/$defs/DvdLookup" },
    { "$ref": "#/$defs/DvdDismiss" },
    { "$ref": "#/$defs/DvdStartRip" },
    { "$ref": "#/$defs/DvdCancelRip" },
    { "$ref": "#/$defs/DvdEject" },
    { "$ref": "#/$defs/DvdLibraryList" },
    { "$ref": "#/$defs/DvdSetOmdbKey" },
    { "$ref": "#/$defs/DvdValidateOmdbKey" },
    { "$ref": "#/$defs/DvdRefreshPosters" },

    { "$ref": "#/$defs/MusicGetStatus" },
    { "$ref": "#/$defs/MusicRefreshStatus" },
    { "$ref": "#/$defs/MusicLookup" },
    { "$ref": "#/$defs/MusicSearch" },
    { "$ref": "#/$defs/MusicDismiss" },
    { "$ref": "#/$defs/MusicStartRip" },
    { "$ref": "#/$defs/MusicCancelRip" },
    { "$ref": "#/$defs/MusicEject" },
    { "$ref": "#/$defs/MusicLibraryList" },
    { "$ref": "#/$defs/MusicRefreshArt" },

    { "$ref": "#/$defs/TelemetryGet" },
    { "$ref": "#/$defs/TelemetryLightsRefresh" },
    { "$ref": "#/$defs/TelemetryLightsSet" },
    { "$ref": "#/$defs/TelemetryLightsSetAll" },
    { "$ref": "#/$defs/TimeSyncStatus" }
  ],

  "$defs": {

    "_NoValue": {
      "type": "object",
      "additionalProperties": false,
      "required": ["action"]
    },
    "_AnyValue": {
      "type": "object",
      "additionalProperties": false,
      "required": ["action"],
      "properties": { "action": { "type": "string" }, "value": {} }
    },

    "SystemEcho": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.echo" }, "value": {} }
    },
    "SystemCommands": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.commands" } }
    },
    "SystemLaunchGui": {
      "x-class": "A", "x-dbc": "PlaybillSystemCmd", "x-dbc-action": "LaunchGui",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.launchGui" } }
    },
    "SystemQuitGui": {
      "x-class": "A", "x-dbc": "PlaybillSystemCmd", "x-dbc-action": "QuitGui",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.quitGui" } }
    },
    "SystemFocus": {
      "x-class": "A", "x-dbc": "PlaybillSystemCmd", "x-dbc-action": "Focus",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.focus" } }
    },
    "SystemWake": {
      "x-class": "A", "x-dbc": "PlaybillSystemCmd", "x-dbc-action": "Wake",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.wake" } }
    },
    "SystemSleep": {
      "x-class": "A", "x-dbc": "PlaybillSystemCmd", "x-dbc-action": "Sleep",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "system.sleep" } }
    },

    "SettingsGet": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "settings.get" } }
    },
    "SettingsReplace": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": { "action": { "const": "settings.replace" }, "value": { "type": "object" } }
    },
    "SettingsPatch": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": { "action": { "const": "settings.patch" }, "value": { "type": "object" } }
    },

    "ConnectionGet": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "connection.get" } }
    },
    "ConnectionSet": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "connection.set" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "required": ["username", "password"],
          "properties": {
            "brokerUrl": { "type": "string", "format": "uri" },
            "username":  { "type": "string", "minLength": 1 },
            "password":  { "type": "string", "minLength": 1 },
            "tlsCertHostname": { "type": ["string", "null"] },
            "caCertProvided":  { "type": "boolean" }
          }
        }
      }
    },
    "ConnectionClear": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "connection.clear" } }
    },
    "ConnectionSetCa": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "connection.setCa" },
        "value":  { "type": "string", "minLength": 26 }
      }
    },

    "NavDpad": {
      "x-class": "A", "x-dbc": "PlaybillNavCmd",
      "description": "Remote/keyboard navigation key. Vocabulary matches the Argon IR Remote and the model documented in docs/app/navigation.md. up/down/left/right are spatial. select activates the focused element. back returns to the previous screen. home returns to home. menu is RESERVED for future contextual options (Roku-Star pattern) and is currently a no-op in the renderer.",
      "type": "object", "additionalProperties": false, "required": ["action", "key"],
      "properties": {
        "action": { "const": "nav.dpad" },
        "key": { "enum": ["up", "down", "left", "right", "select", "back", "home", "menu"] }
      }
    },

    "NavText": {
      "x-class": "B",
      "description": "Stream text into the focused field on the Playbill GUI. The renderer applies the string to document.activeElement when it's an <input>/<textarea>; otherwise it synthesizes per-character keydown events so state-machine screens (e.g. the radio ZIP entry) receive it identically to a USB keyboard. Special characters: '\\b' = Backspace, '\\n' = Enter / submit, '\\t' = Tab. MQTT-only — won't fit in an 8-byte CAN frame.",
      "type": "object", "additionalProperties": false, "required": ["action", "text"],
      "properties": {
        "action": { "const": "nav.text" },
        "text":   { "type": "string", "maxLength": 1024 }
      }
    },

    "TransportPlay": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Play",
      "description": "Two shapes: {sourceId,itemId} for resolve-then-play, OR {url,audioUrl?,headers?,mediaType?,metadata?} for a pre-resolved Playable. audioUrl is the separate audio stream when the source returned video + audio separately (YouTube high-quality path).",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action":    { "const": "transport.play" },
        "sourceId":  { "type": "string" },
        "itemId":    { "type": "string" },
        "url":       { "type": "string", "minLength": 1 },
        "audioUrl":  { "type": ["string", "null"], "minLength": 1 },
        "headers":   { "type": "object" },
        "mediaType": { "enum": ["video", "audio"] },
        "metadata":  { "type": "object" }
      }
    },
    "TransportPause": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Pause",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.pause" } }
    },
    "TransportStop": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Stop",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.stop" } }
    },
    "TransportToggle": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Toggle",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.toggle" } }
    },
    "TransportNext": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Next",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.next" } }
    },
    "TransportPrevious": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "Previous",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.previous" } }
    },
    "TransportSeekRel": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "SeekRel",
      "type": "object", "additionalProperties": false, "required": ["action", "deltaMs"],
      "properties": {
        "action":  { "const": "transport.seekRel" },
        "deltaMs": { "type": "integer", "minimum": -2147483648, "maximum": 2147483647 }
      }
    },
    "TransportSeekAbs": {
      "x-class": "A", "x-dbc": "PlaybillTransportCmd", "x-dbc-action": "SeekAbs",
      "type": "object", "additionalProperties": false, "required": ["action", "positionMs"],
      "properties": {
        "action":     { "const": "transport.seekAbs" },
        "positionMs": { "type": "integer", "minimum": 0, "maximum": 4294967295 }
      }
    },
    "TransportVolumeUp": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "Up",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "transport.volumeUp" },
        "step":   { "type": "integer", "minimum": 1, "maximum": 100, "default": 5 }
      }
    },
    "TransportVolumeDown": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "Down",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "transport.volumeDown" },
        "step":   { "type": "integer", "minimum": 1, "maximum": 100, "default": 5 }
      }
    },
    "TransportVolumeSet": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "Set",
      "type": "object", "additionalProperties": false, "required": ["action", "percent"],
      "properties": {
        "action":  { "const": "transport.volumeSet" },
        "percent": { "type": "integer", "minimum": 0, "maximum": 100 }
      }
    },
    "TransportMuteOn": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "MuteOn",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.muteOn" } }
    },
    "TransportMuteOff": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "MuteOff",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.muteOff" } }
    },
    "TransportMuteToggle": {
      "x-class": "A", "x-dbc": "PlaybillVolumeCmd", "x-dbc-action": "MuteToggle",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "transport.muteToggle" } }
    },

    "RadioTune": {
      "x-class": "A", "x-dbc": "PlaybillRadioTuneReq",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "radio.tune" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "required": ["band", "frequencyHz"],
          "properties": {
            "band":        { "enum": ["fm", "am", "scanner"] },
            "frequencyHz": { "type": "integer", "minimum": 0 },
            "gain":        { "type": ["string", "number", "null"] },
            "modulation":  { "type": ["string", "null"] }
          }
        }
      }
    },
    "RadioStop": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "radio.stop" } }
    },
    "RadioGetState": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "radio.getState" } }
    },
    "RadioScan": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "radio.scan" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "properties": { "band": { "enum": ["fm", "am"] } }
        }
      }
    },
    "RadioListAdapters": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "radio.listAdapters" } }
    },
    "RadioListPresets": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "radio.listPresets" } }
    },
    "RadioSetPresets": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "radio.setPresets" },
        "value":  { "type": "array" }
      }
    },
    "RadioLookupScanner": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "radio.lookupScanner" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "required": ["zip"],
          "properties": { "zip": { "type": "string", "pattern": "^[0-9]{5}$" } }
        }
      }
    },
    "RadioProbeTools": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "radio.probeTools" } }
    },

    "LivetvTune": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "livetv.tune" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "required": ["channel"],
          "properties": {
            "adapter": { "type": "integer", "minimum": 0 },
            "channel": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "LivetvStopTune": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "livetv.stopTune" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "properties": { "adapter": { "type": "integer", "minimum": 0 } }
        }
      }
    },
    "LivetvScan": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "livetv.scan" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "adapter": { "type": "integer", "minimum": 0 },
            "country": { "type": "string", "minLength": 2, "maxLength": 3 }
          }
        }
      }
    },
    "LivetvStopScan": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "livetv.stopScan" } }
    },
    "LivetvListChannels": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "livetv.listChannels" } }
    },
    "LivetvListAdapters": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "livetv.listAdapters" } }
    },
    "LivetvProbeTools": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "livetv.probeTools" } }
    },

    "YoutubeGetSettings": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.getSettings" } }
    },
    "YoutubeSetSettings": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "youtube.setSettings" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "clientId":     { "type": "string", "minLength": 1 },
            "clientSecret": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "YoutubeSignInStart": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.signInStart" } }
    },
    "YoutubeSignInPoll": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.signInPoll" } }
    },
    "YoutubeSignInCancel": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.signInCancel" } }
    },
    "YoutubeSignOut": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.signOut" } }
    },
    "YoutubeGetAccount": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "youtube.getAccount" } }
    },

    "HeadwatersGetSettings": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "headwaters.getSettings" } }
    },
    "HeadwatersSetSettings": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "headwaters.setSettings" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "required": ["apiKey"],
          "properties": {
            "apiKey": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "HeadwatersClear": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "headwaters.clear" } }
    },
    "HeadwatersValidateApiKey": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "headwaters.validateApiKey" },
        "value": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "apiKey": { "type": "string", "minLength": 1 }
          }
        }
      }
    },

    "SourceLaunch": {
      "x-class": "A", "x-dbc": "PlaybillLaunchSourceCmd",
      "type": "object", "additionalProperties": false, "required": ["action", "sourceId"],
      "properties": {
        "action":     { "const": "source.launch" },
        "sourceId":   { "enum": ["youtube", "livetv", "radio", "local", "plex", "spotify", "netflix", "cast"] },
        "subScreen":  { "enum": ["default", "signin", "settings", "search"], "default": "default" }
      }
    },

    "CastStart": {
      "x-class": "B",
      "description": "Start the AirPlay receiver (UxPlay). Idempotent — calling while already running returns the current status. The receiver advertises itself on the LAN as the device's display name (Settings → Device → Name).",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "cast.start" } }
    },
    "CastStop": {
      "x-class": "B",
      "description": "Stop the AirPlay receiver. SIGTERM with a 600ms grace then SIGKILL. Idempotent.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "cast.stop" } }
    },
    "CastGetStatus": {
      "x-class": "B",
      "description": "Returns the current receiver state: {running,state,clientName,startedAt}. state is one of 'idle'|'waiting'|'connected'|'streaming'.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "cast.getStatus" } }
    },

    "NetflixStart": {
      "x-class": "B",
      "description": "Launch the Netflix kiosk browser (Google Chrome --app=https://www.netflix.com). Idempotent — calling while already running returns the current status. Cookies persist in a dedicated user-data-dir so sign-in survives reboots.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "netflix.start" } }
    },
    "NetflixStop": {
      "x-class": "B",
      "description": "Stop the Netflix kiosk browser. SIGTERM with a 600ms grace then SIGKILL. Idempotent.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "netflix.stop" } }
    },
    "NetflixGetStatus": {
      "x-class": "B",
      "description": "Returns {running, startedAt, lastError}.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "netflix.getStatus" } }
    },
    "SourceList": {
      "x-class": "B",
      "description": "Both fields optional. With no sourceId: list registered sources (the apps grid uses this). With sourceId only: list the source's root. With sourceId+path: list a sub-directory.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action":   { "const": "source.list" },
        "sourceId": { "type": "string" },
        "path":     { "type": "string" }
      }
    },
    "SourceResolve": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "sourceId", "itemId"],
      "properties": {
        "action":   { "const": "source.resolve" },
        "sourceId": { "type": "string" },
        "itemId":   { "type": "string" }
      }
    },
    "SourceSearch": {
      "x-class": "B",
      "type": "object", "additionalProperties": false, "required": ["action", "sourceId", "query"],
      "properties": {
        "action":   { "const": "source.search" },
        "sourceId": { "type": "string" },
        "query":    { "type": "string", "minLength": 1 },
        "limit":    { "type": "integer", "minimum": 1, "maximum": 200 }
      }
    },

    "DvdGetStatus": {
      "x-class": "B",
      "description": "Snapshot of the optical-disc slice: present?, label, prompt, ripping progress.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.getStatus" } }
    },
    "DvdRefreshStatus": {
      "x-class": "B",
      "description": "Force a watcher probe right now. Useful when the user clicks 'Check for disc' in the GUI for a disc that was already loaded at boot.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.refreshStatus" } }
    },
    "DvdLookup": {
      "x-class": "B",
      "description": "Online metadata lookup. Returns null if no API key is configured or no match was found; the GUI then falls back to manual entry.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "dvd.lookup" },
        "value": {
          "type": "object", "additionalProperties": false, "required": ["title"],
          "properties": {
            "title": { "type": "string", "minLength": 1 },
            "year":  { "type": "string", "pattern": "^(19|20)\\d{2}$" }
          }
        }
      }
    },
    "DvdDismiss": {
      "x-class": "B",
      "description": "User clicked 'Not now' on the prompt. Suppresses re-prompting until the disc is ejected.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.dismiss" } }
    },
    "DvdStartRip": {
      "x-class": "B",
      "description": "Begin ripping the inserted disc to the local Playbill Library. Fire-and-forget — progress arrives via state.dvd.ripping patches.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "dvd.startRip" },
        "value": {
          "type": "object", "additionalProperties": false, "required": ["metadata"],
          "properties": {
            "metadata": {
              "type": "object", "additionalProperties": true, "required": ["title"],
              "properties": {
                "title":     { "type": "string", "minLength": 1 },
                "year":      { "type": "string" },
                "kind":      { "enum": ["movie", "show"] },
                "show":      { "type": "string" },
                "season":    { "type": "integer", "minimum": 1 },
                "episode":   { "type": "integer", "minimum": 1 },
                "plot":      { "type": "string" },
                "posterUrl": { "type": "string" },
                "rating":    { "type": "string" },
                "runtime":   { "type": "string" },
                "imdbId":    { "type": "string" },
                "source":    { "type": "string" }
              }
            }
          }
        }
      }
    },
    "DvdCancelRip": {
      "x-class": "B",
      "description": "SIGTERM the in-flight HandBrakeCLI. Leaves a partial .mkv on disk; the library scanner filters out files without a sidecar so it doesn't appear in the library.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.cancelRip" } }
    },
    "DvdEject": {
      "x-class": "B",
      "description": "eject(1) the optical drive. Fires after rip completion if the user opts in via the GUI.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.eject" } }
    },
    "DvdLibraryList": {
      "x-class": "B",
      "description": "Return the on-disk library — { movies: [...], shows: [...] }. Scanned every call (cheap; folder-per-title layout).",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.libraryList" } }
    },
    "LibraryDiskInfo": {
      "x-class": "B",
      "description": "Return disk capacity / free space for the partition that holds ~/Videos/Playbill Library. Used by the Offline Library UI to show actual remaining storage instead of a mock '1.2 TB available' string. Returns { totalBytes, freeBytes, mountpoint } or null on failure.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "library.diskInfo" } }
    },
    "DvdSetOmdbKey": {
      "x-class": "B",
      "description": "Persist the user's OMDb API key (https://www.omdbapi.com). Free tier is 1000 lookups/day — plenty for a personal library. Empty value clears.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "dvd.setOmdbKey" },
        "value": {
          "type": "object", "additionalProperties": false,
          "properties": { "apiKey": { "type": "string" } }
        }
      }
    },
    "DvdValidateOmdbKey": {
      "x-class": "B",
      "description": "Validate an OMDb API key (or whichever key is currently stored if 'value' is omitted) by doing a no-op lookup. Used by Settings → Library before persisting.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": {
        "action": { "const": "dvd.validateOmdbKey" },
        "value": {
          "type": "object", "additionalProperties": false,
          "properties": { "apiKey": { "type": "string" } }
        }
      }
    },
    "DvdRefreshPosters": {
      "x-class": "B",
      "description": "Walk the on-disk library and download any missing poster .jpg files using the remote URL stored in each title's JSON sidecar. Used to backfill posters for rips made before poster-caching landed, and to retry titles where the original download failed (no internet at rip time).",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "dvd.refreshPosters" } }
    },

    "MusicGetStatus": {
      "x-class": "B",
      "description": "Snapshot of the audio-CD slice: present?, discid, prompt, rip progress.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.getStatus" } }
    },
    "MusicRefreshStatus": {
      "x-class": "B",
      "description": "Force a CD-watcher probe right now. Mirrors dvd.refreshStatus.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.refreshStatus" } }
    },
    "MusicLookup": {
      "x-class": "B",
      "description": "MusicBrainz TOC-based lookup using the currently-loaded disc's table of contents. Returns ok:false if the disc isn't in MB (rare/promo pressings); the GUI then falls back to music.search.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.lookup" } }
    },
    "MusicSearch": {
      "x-class": "B",
      "description": "MusicBrainz text search by album + artist. Used when music.lookup returned not-found and the user types a guess.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "music.search" },
        "value": {
          "type": "object", "additionalProperties": false,
          "properties": {
            "album":  { "type": "string", "minLength": 1 },
            "artist": { "type": "string", "minLength": 1 }
          }
        }
      }
    },
    "MusicDismiss": {
      "x-class": "B",
      "description": "User clicked 'Not now' on the music prompt. Suppresses re-prompting until the disc is ejected.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.dismiss" } }
    },
    "MusicStartRip": {
      "x-class": "B",
      "description": "Begin ripping the inserted audio CD to the Music Library. Fire-and-forget — per-track progress arrives via state.music.ripping patches.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "music.startRip" },
        "value": {
          "type": "object", "additionalProperties": false, "required": ["metadata"],
          "properties": {
            "metadata": {
              "type": "object", "additionalProperties": true, "required": ["title", "tracks"],
              "properties": {
                "title":   { "type": "string", "minLength": 1 },
                "artist":  { "type": "string" },
                "year":    { "type": "string" },
                "mbid":    { "type": "string" },
                "coverArtUrl": { "type": "string" },
                "country": { "type": "string" },
                "barcode": { "type": "string" },
                "tracks": {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "type": "object", "additionalProperties": true,
                    "required": ["number", "title"],
                    "properties": {
                      "number":     { "type": "integer", "minimum": 1 },
                      "title":      { "type": "string" },
                      "artist":     { "type": "string" },
                      "durationMs": { "type": ["integer", "null"], "minimum": 0 }
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "MusicCancelRip": {
      "x-class": "B",
      "description": "SIGTERM the in-flight cdparanoia/flac. Tracks already ripped stay on disk; the library scanner surfaces them as a partial album.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.cancelRip" } }
    },
    "MusicEject": {
      "x-class": "B",
      "description": "eject(1) the optical drive. Fires after rip completion if the user opts in via the GUI.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.eject" } }
    },
    "MusicLibraryList": {
      "x-class": "B",
      "description": "Return the on-disk music library — { albums: [...] }. Scanned every call.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.libraryList" } }
    },
    "MusicRefreshArt": {
      "x-class": "B",
      "description": "Walk the on-disk music library and download any missing cover.jpg files using the remote coverArtUrl stored in each album's sidecar. Used to backfill albums that were ripped offline.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "music.refreshArt" } }
    },

    "TelemetryGet": {
      "x-class": "B",
      "description": "Return the current state.telemetry snapshot. Used by the Rig view and the live top-bar sysinfo strip.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "telemetry.get" } }
    },
    "TelemetryLightsRefresh": {
      "x-class": "B",
      "description": "Force a fresh fetch of the lights list from Headwaters (HTTP fallback). Normally not needed — retained MQTT config snapshots arrive on connect.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "telemetry.lights.refresh" } }
    },
    "TelemetryLightsSet": {
      "x-class": "B",
      "description": "Toggle / set a single light. Publishes to local/lights/<id>/command which both Headwaters' MQTT bridge and PDM modules listen on. brightness is optional.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "telemetry.lights.set" },
        "value": {
          "type": "object", "additionalProperties": false, "required": ["id"],
          "properties": {
            "id":         { "type": "integer", "minimum": 0 },
            "state":      { "type": "integer", "enum": [0, 1] },
            "brightness": { "type": "integer", "minimum": 0, "maximum": 255 }
          }
        }
      }
    },
    "TelemetryLightsSetAll": {
      "x-class": "B",
      "description": "All-on / all-off bulk light command. Publishes to local/lights/all/command.",
      "type": "object", "additionalProperties": false, "required": ["action", "value"],
      "properties": {
        "action": { "const": "telemetry.lights.setAll" },
        "value": {
          "type": "object", "additionalProperties": false, "required": ["state"],
          "properties": { "state": { "type": "integer", "enum": [0, 1] } }
        }
      }
    },
    "TimeSyncStatus": {
      "x-class": "B",
      "description": "Introspection for the GNSS time-sync handler. Returns whether NTP was disabled, the last time we set the clock, and the remaining cooldown window.",
      "type": "object", "additionalProperties": false, "required": ["action"],
      "properties": { "action": { "const": "time-sync.status" } }
    }
  }
}
