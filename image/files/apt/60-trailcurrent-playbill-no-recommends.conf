// TrailCurrent Playbill — keep image lean by skipping recommends/suggests
// for packages installed during the rsdk customize-hooks. The Ubuntu desktop
// stack itself still gets recommends because we install it via
// `apt install ubuntu-desktop-minimal` which is itself a meta-package.

APT::Install-Recommends "true";
APT::Install-Suggests   "false";
