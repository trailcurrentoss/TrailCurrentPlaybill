# TrailCurrent Playbill — dconf locks for product-identity keys
#
# Lock keys here so first-login (gnome-initial-setup) and per-user dconf
# defaults can't silently overlay our brand-identity choices.
#
# Locking rule of thumb: only lock keys that are PRODUCT IDENTITY (what makes
# this a TrailCurrent device). Do NOT lock keys the user reasonably wants to
# customize — wallpaper, language, keyboard, AND the dock favorites / app
# grid layout. Locking favorite-apps disables ubuntu-dock's "Pin to Dash"
# right-click menu entry, which is a regression vs default Ubuntu behavior.

# Wallpapers — TrailCurrent identity, not a user choice
/org/gnome/desktop/background/picture-uri
/org/gnome/desktop/background/picture-uri-dark
/org/gnome/desktop/background/picture-options
/org/gnome/desktop/background/primary-color
/org/gnome/desktop/background/secondary-color

# Screensaver wallpaper
/org/gnome/desktop/screensaver/picture-uri
/org/gnome/desktop/screensaver/picture-options

# Brand chrome — theme name + icon theme (Playbill identity)
/org/gnome/desktop/interface/gtk-theme
/org/gnome/desktop/interface/icon-theme

# GDM logo
/org/gnome/login-screen/logo
